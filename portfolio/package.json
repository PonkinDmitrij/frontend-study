{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "start": "rm -rf dist && parcel src/index.html",
    "build": "rm -rf dist && parcel build src/index.html --no-source-maps --public-url ./",
    "deploy": "npm run build && gh-pages -d dist -e portfolio"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.2.1",
    "eslint": "^8.8.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-import": "^2.25.4",
    "eslint-plugin-prettier": "^4.0.0",
    "gh-pages": "^3.2.3",
    "parcel": "^2.2.1",
    "prettier": "^2.5.1"
  },
  "dependencies": {
    "normalize.css": "^8.0.1"
  }
}
