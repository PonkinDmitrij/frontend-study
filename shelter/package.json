{
  "name": "shelter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist -e shelter"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "prettier": "^3.3.3",
    "sass": "^1.77.8",
    "vite": "^5.4.1"
  },
  "dependencies": {
    "normalize.css": "^8.0.1"
  }
}
