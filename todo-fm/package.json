{
  "name": "todo-fm",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "start": "rm -rf dist && parcel src/index.html",
    "build": "rm -rf dist && parcel build src/index.html --no-source-maps --public-url ./",
    "deploy": "npm run build && gh-pages -d dist -e todo-fm"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "normalize.css": "^8.0.1"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.4.1",
    "eslint": "^8.13.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-import": "^2.26.0",
    "eslint-plugin-prettier": "^4.0.0",
    "gh-pages": "^3.2.3",
    "parcel": "^2.4.1",
    "prettier": "^2.6.2"
  }
}
